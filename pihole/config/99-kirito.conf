# Split-horizon DNS (homelab-docs architecture/003): every *.kirito.com
# name resolves to the Pi, where Traefik routes by Host header. This file
# is the Git-managed source of truth — do not add records via the UI.
# The IP must match LAN_IP in homelab-bootstrap config/defaults.env.
address=/kirito.com/192.168.1.10
