# traefik — copy to /srv/homelab/secrets/traefik.env on the Pi (RB-002).
# Traefik needs no secrets; the file exists to satisfy the deploy.sh
# contract (every compose stack has a secrets file).
TZ=Europe/Berlin
